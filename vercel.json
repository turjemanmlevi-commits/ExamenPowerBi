{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["index.html"], "helpers": false, "maxDuration": 300 }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server.js" }]
}
